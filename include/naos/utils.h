#ifndef NAOS_UTILS_H
#define NAOS_UTILS_H

#include <stdint.h>

/**
 * Returns the elapsed milliseconds since the start.
 *
 * @return - The elapsed milliseconds.
 */
uint32_t naos_millis();

#endif  // NAOS_UTILS_H
